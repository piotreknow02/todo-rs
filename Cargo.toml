[package]
name = "todo-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0-rc.2", features = ["json"] }
dotenvy = "0.15.6"
mongodb = { version = "2.1.0", default-features = false, features = ["sync"] }